{
  "name": "mt-11ty-template",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "prepare": "git config core.hooksPath .githooks",
    "cv": "cd src/assets/CV && latexmk -lualatex -interaction=nonstopmode -halt-on-error -outdir=build CV.tex && cp build/CV.pdf CV.pdf && rm -rf build"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "fast-xml-parser": "^5.11.0"
  }
}
